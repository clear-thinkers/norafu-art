{
  "name": "norafu-art",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist",
    "add": "node scripts/add-from-csv.mjs",
    "update": "node scripts/add-from-csv.mjs --update"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
